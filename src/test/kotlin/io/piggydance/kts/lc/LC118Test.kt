package io.piggydance.kts.lc

import org.junit.jupiter.api.Test

class LC118Test {
    @Test
    fun `test basic case`() {
        val s = LC118()
        println(s.generate(1))
        println(s.generate(2))
        println(s.generate(3))
        println(s.generate(4))
        println(s.generate(5))
    }
}